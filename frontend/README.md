# Express Your Feelings - Frontend

Современное React приложение для управления задачами с красивым Material-UI интерфейсом и интеграцией с RESTful API.

## 📋 Содержание

- [🚀 Возможности](#-возможности)
- [🛠 Технологии](#-технологии)
- [📁 Структура проекта](#-структура-проекта)
- [🚀 Установка и запуск](#-установка-и-запуск)
- [🎨 Компоненты](#-компоненты)
- [🔗 API Интеграция](#-api-интеграция)
- [🎨 UI/UX Особенности](#-uiux-особенности)
- [🔧 Конфигурация](#-конфигурация)
- [🧪 Тестирование](#-тестирование)
- [📱 Responsive Design](#-responsive-design)
- [🎯 Состояния приложения](#-состояния-приложения)
- [🔄 Жизненный цикл](#-жизненный-цикл)
- [🚀 Производительность](#-производительность)
- [🔧 Разработка](#-разработка)
- [📝 Скрипты](#-скрипты)
- [🔗 Ссылки](#-ссылки)
- [📄 Лицензия](#-лицензия)

## 🚀 Возможности

- ✅ **Создание новых задач** через удобный диалог
- ✅ **Редактирование существующих задач** с валидацией
- ✅ **Удаление задач** с подтверждением
- ✅ **Изменение статуса задач** (Pending, In Progress, Completed)
- ✅ **Современный Material-UI интерфейс** с анимациями
- ✅ **Адаптивный дизайн** для всех устройств
- ✅ **Обработка ошибок** с уведомлениями
- ✅ **Состояния загрузки** с индикаторами
- ✅ **Context API** для глобального управления состоянием
- ✅ **Axios** для надежных HTTP запросов
- ✅ **Валидация форм** на клиенте и сервере

## 🛠 Технологии

- **React 18** - библиотека для создания пользовательских интерфейсов
- **Material-UI (MUI)** - компонентная библиотека для быстрой разработки
- **Axios** - HTTP клиент для API запросов
- **React Context** - управление глобальным состоянием
- **Create React App** - инструменты разработки и сборки

## 📁 Структура проекта

```
frontend/src/
├── components/
│   ├── TaskList.js      # Список задач с сеткой
│   ├── TaskCard.js      # Карточка отдельной задачи
│   └── TaskDialog.js    # Диалог создания/редактирования
├── context/
│   └── TaskContext.js   # Context для управления состоянием задач
├── App.js              # Главный компонент приложения
└── index.js            # Точка входа
```

## 🚀 Установка и запуск

### Предварительные требования
- Node.js >= 16.0.0
- npm >= 8.0.0
- Backend API должен быть запущен на http://localhost:5000

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm start
```

Приложение будет доступно на http://localhost:3000

### Сборка для продакшена
```bash
npm run build
```

### Запуск тестов
```bash
npm test
```

## 🎨 Компоненты

### TaskList
Основной компонент для отображения списка задач:
- Адаптивная сетка карточек
- Состояние загрузки с индикатором
- Обработка ошибок с уведомлениями
- Floating Action Button для добавления задач
- Пустое состояние с подсказками

### TaskCard
Карточка отдельной задачи:
- Отображение названия, описания и статуса
- Цветные индикаторы статуса
- Кнопки редактирования и удаления
- Анимация при наведении
- Подтверждение удаления

### TaskDialog
Диалог для создания и редактирования задач:
- Форма с валидацией
- Поля: название, описание, статус
- Обработка ошибок валидации
- Состояние загрузки при сохранении
- Автофокус на первом поле

### TaskContext
Context для управления состоянием:
- CRUD операции с задачами
- Обработка ошибок API
- Состояния загрузки
- Управление диалогами
- Кэширование данных

## 🔗 API Интеграция

Приложение интегрируется с backend API через следующие endpoints:

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/tasks` | Получение всех задач |
| GET | `/tasks/:id` | Получение задачи по ID |
| POST | `/tasks` | Создание новой задачи |
| PUT | `/tasks/:id` | Обновление задачи |
| DELETE | `/tasks/:id` | Удаление задачи |

### Формат данных

```typescript
interface Task {
  id: string;
  title: string;
  description: string;
  status: 'pending' | 'in progress' | 'completed';
  createdAt: string;
  updatedAt: string;
}
```

## 🎨 UI/UX Особенности

### Material Design
- Соблюдение принципов Material Design
- Консистентная типографика
- Правильные отступы и spacing
- Цветовая схема с акцентами

### Адаптивность
- Мобильная версия с одной колонкой
- Планшетная версия с двумя колонками
- Десктопная версия с тремя колонками
- Адаптивные диалоги

### Анимации
- Плавные переходы между состояниями
- Анимация карточек при наведении
- Fade-in эффекты для диалогов
- Loading индикаторы

### Доступность
- Семантическая разметка
- ARIA атрибуты
- Поддержка клавиатурной навигации
- Контрастные цвета

## 🔧 Конфигурация

### Переменные окружения
Создайте файл `.env` в корне frontend папки:

```env
REACT_APP_API_URL=http://localhost:5000
REACT_APP_ENV=development
```

### Настройка API
API URL настраивается в `src/context/TaskContext.js`:

```javascript
const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000';
```

## 🧪 Тестирование

### Запуск тестов
```bash
npm test
```

### Запуск тестов в watch режиме
```bash
npm test -- --watch
```

### Покрытие кода тестами
```bash
npm test -- --coverage
```

## 📱 Responsive Design

Приложение адаптируется под различные размеры экранов:

- **Mobile (< 600px)**: 1 колонка
- **Tablet (600px - 960px)**: 2 колонки  
- **Desktop (> 960px)**: 3 колонки

## 🎯 Состояния приложения

### Загрузка
- Индикатор загрузки при первом открытии
- Skeleton loader для карточек
- Disabled состояния для кнопок

### Ошибки
- Toast уведомления об ошибках
- Retry механизм для неудачных запросов
- Graceful fallback для критических ошибок

### Пустое состояние
- Информативное сообщение
- Call-to-action кнопка
- Иллюстрация или иконка

## 🔄 Жизненный цикл

1. **Инициализация**: Загрузка задач при монтировании
2. **Создание**: Открытие диалога → Валидация → API запрос → Обновление списка
3. **Редактирование**: Открытие диалога → Заполнение формы → Валидация → API запрос → Обновление карточки
4. **Удаление**: Подтверждение → API запрос → Удаление из списка
5. **Обновление статуса**: Выбор статуса → API запрос → Обновление карточки

## 🚀 Производительность

- **Lazy loading** компонентов
- **Memoization** для предотвращения лишних ререндеров
- **Debounced** поиск (если добавлен)
- **Optimistic updates** для лучшего UX
- **Error boundaries** для изоляции ошибок

## 🔧 Разработка

### Структура компонентов
```
TaskList (Container)
├── TaskCard (Presentational)
│   ├── StatusChip
│   ├── ActionButtons
│   └── TaskContent
└── TaskDialog (Modal)
    ├── TaskForm
    └── DialogActions
```

### Паттерны
- **Container/Presentational** разделение
- **Custom hooks** для переиспользуемой логики
- **Context** для глобального состояния
- **Prop drilling** минимизирован

## 📝 Скрипты

- `npm start` - запуск в режиме разработки
- `npm run build` - сборка для продакшена
- `npm test` - запуск тестов
- `npm run eject` - извлечение конфигурации (необратимо)

## 🔗 Ссылки

- **Приложение**: http://localhost:3000
- **Backend API**: http://localhost:5000
- **Material-UI**: https://mui.com/
- **React**: https://reactjs.org/

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.
